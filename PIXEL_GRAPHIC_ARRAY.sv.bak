module PIXEL_GRAPHIC_ARRAY ();

	genvar i;
	generate
		for(i = 2; i < WIDTH-1; i++) 
		begin : generate_block_identifier
			BIDIMENTIONAL_CELL_ARRAY #(32, 635, 4)
				BCA (CLK, WRITE, RST, ROW, COLUMN, IN_DATA, OUT_DATA);
		end
	endgenerate
	
endmodule 